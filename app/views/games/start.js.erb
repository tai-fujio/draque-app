document.getElementById("battle-status").innerHTML = "<%= j(render 'games/status/battle_status') %>"
battleMonsterImageBg.innerHTML = "<%= j(render 'games/monster/bg') %>"
document.getElementById("battle-monster-name").innerText = "<%= @monster.name %>"
battleMonsterImage.innerHTML = "<%= j(render 'games/monster/image') %>"

battleMonster.style.opacity = "1"
battleMessages.innerHTML = "<%= Message.start(@monster) %>"
battleButtonContainer.style.display = "block"
exitButtonContainer.style.display = "block"
<%# skillButtonContainer.style.display = "block" %>
spellButtonContainer.style.display = "block"

var BraveLevel = <%= @brave.level %>

<%# if(BraveLevel >= 2){
    skillButton.classList.remove("display-none-btn")
    skillButton.classList.add("open-btn")
} %>

if(BraveLevel >= 2){
  spellButton.classList.remove("display-none-btn")
  spellButton.classList.add("open-btn")
}


<%# _headのActionボタン押したら、まず空のテキストをsercretボタンを自動で押して送信。そして５秒後に隠れているspellボタンが押され起動。 %>
<%# function actClick(){}
function secretClick(){} %>

document.getElementById("spell-button").onclick = function() {
  setTimeout(spellClick, 5000);
  document.getElementById("secret-btn").click();
}

function spellClick() {
  document.getElementById("action-btn").click();
}

<%# secretボタンが押されるとテキストの中身を空に %>
$(function () {
  $("#secret-btn").click( function() {
    $("#text-btn").val("");
  });
});




<%# document.getElementById('#text-box').focus(); %>
<%# document.brave.brave[word].focus() %>

<%# startアクションの際起動 %>
